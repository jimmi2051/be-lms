{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "defaultConnection": "default",
      "connections": {
        "default": {
          "connector": "mongoose",
          "settings": {
            "uri": "${process.env.DB_URI || ''}"
          },
          "options": {
            "ssl": true
          }
        }
      }
    }
  }
}
