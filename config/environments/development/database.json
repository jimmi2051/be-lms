{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose",
      "settings": {
        "database": "be-lms",
        "host": "${process.env.APP_HOST || 'ds249818.mlab.com'}",
        "port": "${process.env.NODE_PORT || 49818}",
        "srv": false,
        "username": "root",
        "password": "o0i9u8y7"
      },
      "options": {
        "authenticationDatabase": "be-lms",
        "ssl": false
      }
    }
  }
}
