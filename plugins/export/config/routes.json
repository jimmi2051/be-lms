{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "Export.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/",
      "handler": "Export.export",
      "config": {
        "policies": []
      }
    }
  ]
}